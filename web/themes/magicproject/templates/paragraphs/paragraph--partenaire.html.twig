{% if content.field_url_partenaire[0] %}
    <div class="col-xl-4 col-md-6 col-xs-12 align-middle"><a href="{{ content.field_url_partenaire }}">
            {{ content.field_logo_partenaire }}
        </a>
    </div>
{% else %}
    <div class="col-xl-4 col-md-6 col-xs-12">
        {{ content.field_logo_partenaire }}
    </div>
{% endif %}